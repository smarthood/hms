import 'package:flutter/material.dart';
class amb extends StatelessWidget {
  const amb({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Column(
            children: [
              Text('ambulance')
            ],
          )
      ),
    );
  }
}
