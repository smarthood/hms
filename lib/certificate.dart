import 'package:flutter/material.dart';
class certi extends StatelessWidget {
  const certi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Column(
            children: [
              Text('Certificate')
            ],
          )
      ),
    );
  }
}
