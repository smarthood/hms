import 'package:flutter/material.dart';
class pharm extends StatelessWidget {
  const pharm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Column(
            children: [
              Text('pharmacy')
            ],
          )
      ),
    );
  }
}
