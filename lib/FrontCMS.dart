import 'package:flutter/material.dart';
class frontcms extends StatelessWidget {
  const frontcms({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Column(
            children: [
              Text('FrontCMS')
            ],
          )
      ),
    );
  }
}
