import 'package:flutter/material.dart';
class tpa extends StatelessWidget {
  const tpa({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Column(
            children: [
              Text('TPA Management')
            ],
          )
      ),
    );
  }
}
